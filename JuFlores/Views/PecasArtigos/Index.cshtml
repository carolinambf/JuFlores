@model IEnumerable<JuFlores.Models.PecasArtigos>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Peca)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Qtd)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Artigo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Peca.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Qtd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Artigo.Criador)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PecaFK">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PecaFK">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PecaFK">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
